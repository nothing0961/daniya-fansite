/**
 * Footer — 站点底部栏
 * 已清空内容，背景与首页同色（#0f0610）
 */

export function Footer() {
  return (
    <footer
      className="relative z-10 border-t border-transparent"
      style={{ backgroundColor: "transparent" }}
    >
      <div className="mx-auto max-w-4xl px-4 py-6">
        {/* 内容已清空，预留位置 */}
      </div>
    </footer>
  );
}
